import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import api from '../services/api';const cards=[['available_stock','Available stock'],['today_sales',"Today's sales"],['today_profit',"Today's profit"],['pending_requests','Open requests'],['stock_value','Stock value']];export default function Dashboard(){const [s,setS]=useState({});useEffect(()=>{const load=()=>api.get('/dashboard/stats.php').then(r=>setS(r.stats));load();const t=setInterval(load,9000);return()=>clearInterval(t)},[]);return <><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div><h2 className="text-3xl font-bold">Dashboard</h2><p className="text-slate-500">Updated every 9 seconds.</p></div><Link className="btn-primary md:hidden" to="/sales">Quick sale</Link></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5">{cards.map(([k,l])=><div className="card" key={k}><p className="text-sm text-slate-500">{l}</p><b className="mt-2 block text-2xl">{k.includes('profit')||k.includes('value')?`TZS ${Number(s[k]||0).toLocaleString()}`:s[k]||0}</b></div>)}</div><Link className="btn-primary mt-6 hidden md:inline-block" to="/sales">Quick sale</Link></>}
